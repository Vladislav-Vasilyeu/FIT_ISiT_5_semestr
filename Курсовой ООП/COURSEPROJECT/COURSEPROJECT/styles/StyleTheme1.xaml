<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Блок для темы -->

    <Color x:Key="PrimaryAdminLite">#8f8f8f</Color>
    <Color x:Key="PrimaryAdmin">#616161</Color>
    <Color x:Key="PrimaryBlack">#0c1338</Color>
    <Color x:Key="Primary">#2B59C3</Color>
    <Color x:Key="PrimaryLight">#7FD1B9</Color>
    <Color x:Key="PrimaryAnim">#EBBAB9</Color>
    <Color x:Key="InputError">#FF6B6B</Color>

    <ImageBrush x:Key="mainfon" ImageSource="/images/fon1.jpg" Stretch="UniformToFill"/>
    <BitmapImage x:Key="DynamicGifSource" UriSource="/images/animfon.gif"/>

    <SolidColorBrush x:Key="PrimaryAdminLiteBrush" Color="{StaticResource PrimaryAdminLite}"/>
    <SolidColorBrush x:Key="PrimaryAdminBrush" Color="{StaticResource PrimaryAdmin}"/>
    <SolidColorBrush x:Key="PrimaryBlackBrush" Color="{StaticResource PrimaryBlack}"/>
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Primary}"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLight}"/>
    <SolidColorBrush x:Key="PrimaryAnimBrush" Color="{StaticResource PrimaryAnim}"/>
    <SolidColorBrush x:Key="InputErrorBrush" Color="{StaticResource InputError}"/>

    <!-- ToolTip -->
    <Style x:Key="CustomToolTip" TargetType="ToolTip">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}" />
        <Setter Property="Foreground" Value="Black" />
    </Style>

    <!-- Стили для всех кнопок -->
    <Style x:Key="TransparentButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стили для MainWindow -->
    <Style x:Key="RoundedBorderStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="Gray"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
    </Style>

    <Style x:Key="TransparentTextBoxStyle" TargetType="TextBox">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="24"/>
    </Style>

    <Style x:Key="TextBoxDataTrigger" TargetType="TextBox" BasedOn="{StaticResource TransparentTextBoxStyle}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Text.Length, RelativeSource={RelativeSource Self}}" Value="20">
                <Setter Property="Foreground" Value="#d92952"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="LanguageButtonStyle" TargetType="Button" BasedOn="{StaticResource TransparentButtonStyle}">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="10 5 10 5"/>
    </Style>

    <Style x:Key="AnimationLangBtn" TargetType="Button" BasedOn="{StaticResource LanguageButtonStyle}">
        <Setter Property="Margin" Value="10 0 0 0"/>
        <Setter Property="MaxHeight" Value="50"/>
        <Setter Property="Padding" Value="10 0 10 0"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                        Storyboard.TargetProperty="RenderTransform.ScaleX"
                        To="1.05" 
                        Duration="0:0:0.2"/>
                        <DoubleAnimation
                        Storyboard.TargetProperty="RenderTransform.ScaleY"
                        To="1.05" 
                        Duration="0:0:0.2"/>
                        <DoubleAnimation 
                        Storyboard.TargetProperty="Opacity"
                        To="0.9" 
                        Duration="0:0:0.2"/>
                        <ColorAnimation
                        Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)"
                        To="{StaticResource PrimaryAnim}"
                        Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                        Storyboard.TargetProperty="RenderTransform.ScaleX"
                        To="1.0" 
                        Duration="0:0:0.2"/>
                        <DoubleAnimation
                        Storyboard.TargetProperty="RenderTransform.ScaleY"
                        To="1.0" 
                        Duration="0:0:0.2"/>
                        <DoubleAnimation 
                        Storyboard.TargetProperty="Opacity"
                        To="1" 
                        Duration="0:0:0.2"/>
                        <ColorAnimation
                        Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)"
                        To="{StaticResource Primary}"
                        Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="UserButtonStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.9" Duration="0:0:0.2"/>
                        <ColorAnimation Storyboard.TargetProperty="Background.Color" 
                                  To="{StaticResource PrimaryAnim}"
                                  Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                        <ColorAnimation Storyboard.TargetProperty="Background.Color" 
                                  To="{StaticResource Primary}"
                                  Duration="0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>


    <Style x:Key="UserStackPanelStyle" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Height" Value="50"/>
    </Style>

    <Style x:Key="UserNameTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="10 0 10 0"/>
    </Style>

    <!-- Стили для AdminTable -->

    <Style x:Key="GridAdminStyle" TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource PrimaryAdminBrush}"/>
    </Style>
    <Style x:Key="GridDataAdminStyle" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource PrimaryAdminBrush}"/>
        <Setter Property="RowBackground" Value="{StaticResource PrimaryAdminLiteBrush}"/>
    </Style>

    <!-- Стили для mapbooking -->
 
    <Style x:Key="GridStyle" TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource PrimaryBlackBrush}"/>
    </Style>
    <Style x:Key="CanvasStyle" TargetType="Canvas">
        <Setter Property="Background" Value="{StaticResource PrimaryBlackBrush}"/>
    </Style>

    <!-- Стили для AddItem -->
    <Style x:Key="AddItemStackPanelStyle" TargetType="StackPanel">
        <Setter Property="MaxWidth" Value="300"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Opacity" Value="0.9"/>
    </Style>

    <Style x:Key="AddItemTextBlockStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="10 10 10 0"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="AddItemTextBoxStyle" TargetType="TextBox">
        <Setter Property="Margin" Value="10 10 10 0"/>
    </Style>

    <Style x:Key="AddItemButtonStyle" TargetType="Button" BasedOn="{StaticResource TransparentButtonStyle}">
        <Setter Property="Margin" Value="10 10 10 10"/>
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="white"/>
    </Style>

    <!-- Стили для authorization -->
    <Style x:Key="GridAuthorization" TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
    </Style>
    <Style x:Key="AuthorizationBorderStyle" TargetType="Border">
        <Setter Property="MaxWidth" Value="200"/>
        <Setter Property="Height" Value="Auto"/>
    </Style>
    
    <Style x:Key="PersonalBorderStyle" TargetType="Border">
        <Setter Property="MaxWidth" Value="280"/>
        <Setter Property="Height" Value="Auto"/>
    </Style>
    

    <Style x:Key="AuthorizationStackPanelStyle" TargetType="StackPanel">
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="AuthorizationTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="AuthorizationLabelTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="10 0 0 5"/>
    </Style>

    <Style x:Key="AuthorizationInputBorderStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Margin" Value="10 0 10 10"/>
    </Style>

    <Style x:Key="AuthorizationTextBoxStyle" TargetType="TextBox">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <Style x:Key="AuthorizationPasswordBoxStyle" TargetType="PasswordBox">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <Style x:Key="AuthorizationButtonStyle" TargetType="Button" BasedOn="{StaticResource TransparentButtonStyle}">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Margin" Value="10 0 10 10"/>
    </Style>

    <Style TargetType="Button" x:Key="AuthorizationButtonStyleTrigger" BasedOn="{StaticResource AuthorizationButtonStyle}">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <MultiTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                            Storyboard.TargetProperty="RenderTransform.ScaleX"
                            To="1.03" 
                            Duration="0:0:0.2"/>
                            <DoubleAnimation 
                            Storyboard.TargetProperty="RenderTransform.ScaleY"
                            To="1.15" 
                            Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.EnterActions>
                <MultiTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                            Storyboard.TargetProperty="RenderTransform.ScaleX"
                            To="1" 
                            Duration="0:0:0.2"/>
                            <DoubleAnimation 
                            Storyboard.TargetProperty="RenderTransform.ScaleY"
                            To="1" 
                            Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.ExitActions>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!-- Стили для Editing -->
    <Style x:Key="EditingPopupBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
        <Setter Property="BorderBrush" Value="Black"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10"/>
    </Style>

    <Style x:Key="EditingPopupImageStyle" TargetType="Image">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
    </Style>

    <Style x:Key="EditingPopupTextBoxStyle" TargetType="TextBox">
        <Setter Property="Margin" Value="0 5 0 5"/>
    </Style>

    <Style x:Key="EditingPopupButtonStyle" TargetType="Button" BasedOn="{StaticResource TransparentButtonStyle}">
        <Setter Property="Margin" Value="0 10 0 0"/>
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="white"/>
    </Style>

    <!-- Стили для Filtering -->
    <Style x:Key="FilteringSearchBorderStyle" TargetType="Border">
        <Setter Property="Width" Value="300"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Margin" Value="30 0 0 0"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="Gray"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
    </Style>

    <Style x:Key="FilteringSearchTextBoxStyle" TargetType="TextBox">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="24"/>
    </Style>

    <Style x:Key="FilteringSearchImageStyle" TargetType="Image">
        <Setter Property="Width" Value="60"/>
        <Setter Property="Margin" Value="10 0 0 0"/>
    </Style>

    <Style x:Key="FilteringPopupButtonStyle" TargetType="Button" BasedOn="{StaticResource TransparentButtonStyle}">
        <Setter Property="MaxWidth" Value="150"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Background" Value="{StaticResource PrimaryAnimBrush}"/>
        <Setter Property="Foreground" Value="white"/>
        <Setter Property="Padding" Value="5" />
    </Style>

    <Style x:Key="FilteringPopupBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="CornerRadius" Value="5"/>
        <Setter Property="Padding" Value="10"/>
    </Style>

    <Style x:Key="FilteringPopupTextBlockStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Margin" Value="5"/>
    </Style>

    <Style x:Key="FilteringPopupTextBoxStyle" TargetType="TextBox">
        <Setter Property="Width" Value="100"/>
        <Setter Property="Margin" Value="5"/>
    </Style>

    <Style x:Key="FilteringPopupApplyButtonStyle" TargetType="Button" BasedOn="{StaticResource TransparentButtonStyle}">
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
    </Style>
    
    <!-- Стили для ThemeButton -->
    <ControlTemplate x:Key="ThemeButtonTemplate" TargetType="Button">
        <Grid>
            <Path x:Name="hexagon" 
              Fill="{TemplateBinding Background}"
              Stroke="{TemplateBinding BorderBrush}"
              StrokeThickness="{TemplateBinding BorderThickness}">
                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="12,0">
                            <LineSegment Point="140,0"/>
                            <LineSegment Point="140,33.33"/>
                            <LineSegment Point="128,50"/>
                            <LineSegment Point="0,50"/>
                            <LineSegment Point="0,16"/>
                            <LineSegment Point="12,0"/>
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
            </Path>
    
            <ContentPresenter HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>
        </Grid>
    </ControlTemplate>

    <!-- Стили для LangButton -->
    <ControlTemplate x:Key="LangButtonTemplate" TargetType="Button">
        <Grid>
            <Path x:Name="hexagon" 
           Fill="{TemplateBinding Background}"
           Stroke="{TemplateBinding BorderBrush}"
           StrokeThickness="{TemplateBinding BorderThickness}">
                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="12,0">
                            <LineSegment Point="90,0"/>
                            <LineSegment Point="90,33.33"/>
                            <LineSegment Point="78,50"/>
                            <LineSegment Point="0,50"/>
                            <LineSegment Point="0,16"/>
                            <LineSegment Point="12,0"/>
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
            </Path>

            <ContentPresenter HorizontalAlignment="Center"
                     VerticalAlignment="Center"/>
        </Grid>
    </ControlTemplate>
    
    <!-- Стили для WritingReview -->

    <Style x:Key="WritingReviewFon" TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
    </Style>

    <ControlTemplate x:Key="SaveReviewButtonTemplate" TargetType="Button">
        <Grid>
            <Path x:Name="hexagon" 
       Fill="{TemplateBinding Background}"
       Stroke="{TemplateBinding BorderBrush}"
       StrokeThickness="{TemplateBinding BorderThickness}">
                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="18,0">
                            <LineSegment Point="135,0"/>
                            <LineSegment Point="135,50"/>
                            <LineSegment Point="117,75"/>
                            <LineSegment Point="0,75"/>
                            <LineSegment Point="0,24"/>
                            <LineSegment Point="18,0"/>
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
            </Path>

            <ContentPresenter HorizontalAlignment="Center"
                 VerticalAlignment="Center"/>
        </Grid>
    </ControlTemplate>

</ResourceDictionary>